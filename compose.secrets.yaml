services:
  api:
    environment:
      FPT_AI_API_KEY_FILE: /run/secrets/fpt_ai_api_key
    secrets:
      - fpt_ai_api_key

  realtime:
    environment:
      FPT_AI_API_KEY_FILE: /run/secrets/fpt_ai_api_key
    secrets:
      - fpt_ai_api_key

secrets:
  fpt_ai_api_key:
    external: true
    name: tourtect_fpt_ai_api_key
