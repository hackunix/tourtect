import { getDictionary,getLocale } from "@/lib/i18n";import AppShell from "@/components/AppShell";import styles from "@/components/community.module.css";
export default async function ProfilePage(){const dict=getDictionary(await getLocale());return <AppShell dict={dict}><main className={styles.page}><header className={styles.pageHeader}><h1>{dict.profile}</h1></header><div className={styles.pageBody}><div className={styles.avatar}>S</div><h2>Seed User Alpha</h2><p className={styles.notice}>Vertical-slice demo identity. Đây không phải phiên đăng nhập production.</p></div></main></AppShell>}
